name: family_task
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  charts_flutter: ^0.10.0
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^9.0.0
  google_fonts: ^2.0.0
  hive: ^2.0.3
  hive_flutter: ^1.0.0
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.12.2
  flutter_launcher_icons: ^0.9.0
  flutter_native_splash: ^1.1.8+4
  hive_generator: ^1.0.1
  image: ^3.0.2
  image_picker: ^0.7.4
  # intl: ^0.17.0
  path_provider: ^2.0.1

flutter_native_splash:
  color: "#C6B5ED"
  image: "Images/budget.png"
  android: true
  ios: true

flutter_icons:
  ios: true
  android: "launch_icon"
  image_path: "Images/budget.png"

flutter:
  uses-material-design: true

  assets:
    - Images/
    - Images/Background/
